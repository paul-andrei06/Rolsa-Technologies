<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - RolsaTechnologies</title>
    <script type="importmap"></script>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/RolsaTechnologies.styles.css" asp-append-version="true" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css">
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-light bg-white border-bottom box-shadow mb-3">
            <div class="container-fluid">
                <a class="navbar-brand" asp-area="" asp-controller="Home" asp-action="Index">
                    <img src="/Images/Logo.png" alt="RolsaTechnologies" height="100" width="150" />
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse" aria-controls="navbarSupportedContent"
                        aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="navbar-collapse collapse d-sm-inline-flex justify-content-between">
                    <ul class="navbar-nav flex-grow-1">
                        
                        <!-- Admin Button - Visible only if the user has an Admin Role -->
                        @if (User.IsInRole("Admin"))
                        {
                            <li class="nav-item">
                                <a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Admin">Admin</a>
                            </li>
                        }
                        
                        <li class="nav-item">
                            <a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Index">Home</a>
                        </li>

                        <!-- Information Dropdown -->
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle text-dark" id="navbarDropdownInformation" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                Information
                            </a>
                            <ul class="dropdown-menu" aria-labelledby="navbarDropdownInformation">
                                <li><a class="dropdown-item" asp-area="" asp-controller="Home" asp-action="GreenEnergyProducts">Green Energy Products</a></li>
                                <li><a class="dropdown-item" asp-area="" asp-controller="Home" asp-action="Privacy">Privacy</a></li>
                                <li><a class="dropdown-item" asp-area="" asp-controller="Home" asp-action="AboutUs">About</a></li>
                                
                                <!-- Add more links as needed -->
                            </ul>
                        </li>

                        <!-- Tools Dropdown -->
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle text-dark" id="navbarDropdownTools" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                Tools
                            </a>
                            <ul class="dropdown-menu" aria-labelledby="navbarDropdownTools">
                                <li><a class="dropdown-item" asp-area="" asp-controller="Calculators" asp-action="Index">Calculator</a></li>
                                <li><a class="dropdown-item" asp-area="" asp-controller="EnergyTrackers" asp-action="Index">EnergyTracker</a></li>
                                <!-- Add more tools as needed -->
                            </ul>
                        </li>

                        <!-- Services Dropdown -->
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle text-dark" id="navbarDropdownServices" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                Services
                            </a>
                            <ul class="dropdown-menu" aria-labelledby="navbarDropdownServices">
                                <li><a class="dropdown-item" asp-area="" asp-controller="ScheduleConsultations" asp-action="Index">Consultations</a></li>
                                <li><a class="dropdown-item" asp-area="" asp-controller="ScheduleInstallations" asp-action="Index">Installations</a></li>
                                <li><a class="dropdown-item" asp-area="" asp-controller="Home" asp-action="FAQs">FAQ</a></li>
                                <!-- Add more services as needed -->
                            </ul>
                        </li>

                        <!-- Accessibility Dropdown -->
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle text-dark" id="navbarDropdownAccessibility" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                Accessibility
                            </a>
                            <ul class="dropdown-menu" aria-labelledby="navbarDropdownAccessibility">
                                <li><a class="dropdown-item" href="#" onclick="toggleDarkMode()">Toggle Dark Mode</a></li>
                                <li><a class="dropdown-item" href="#" onclick="toggleHighContrast()">Toggle High Contrast</a></li>
                                <li><a class="dropdown-item" href="#" onclick="changeFontSize('increase')">Increase Font Size</a></li>
                                <li><a class="dropdown-item" href="#" onclick="changeFontSize('decrease')">Decrease Font Size</a></li>
                            </ul>
                        </li>

                    </ul>
                    <partial name="_LoginPartial" />
                </div>
            </div>
        </nav>
    </header>

    <style>

        html, body {
            height: 100%;
            margin: 0;
        }

        /* Flexbox for body, ensures the footer stays at the bottom */
        body {
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        /* Push content to take available space */
        .content {
            flex: 1;
        }

        footer {
            background-color: #f8f9fa;
            padding: 20px 0;
        }
    </style>

        <div class="content">
            <div class="container">
                <main role="main" class="pb-3">
                    @RenderBody()
                </main>
            </div>
        </div>

    <!-- Footer Section -->
    <footer>
        <div class="container">
            <div class="row justify-content-center">

                <!-- Useful Links Section -->
                <div class="col-sm-4 col-md-3 item">
                    <h3>Useful Links</h3>
                    <ul class="list-unstyled">
                        <!-- List of important pages on the site -->
                        <li><a asp-controller="Home" asp-action="Privacy">Privacy Policy</a></li>
                        <li><a asp-controller="Home" asp-action="FAQs">FAQs</a></li>
                        <li><a asp-controller="Home" asp-action="Index">Accessibility</a></li>
                    </ul>
                </div>

                <!-- Contact Information Section -->
                <div class="col-sm-4 col-md-3 item">
                    <h3>Contact</h3>
                    <ul class="list-unstyled">
                        <!-- Contact details with icons -->
                        <li><i class="bi bi-telephone"></i> +44 123 456 7890</li>
                        <li><i class="bi bi-envelope"></i> info@rolsatechnologies.com</li>
                    </ul>
                </div>

                <!-- Social Media and Copyright Section -->
                <div class="col-lg-3 item social text-center">
                    <!-- Social media links with icons -->
                    <a href="#"><i class="icon ion-social-facebook fs-4"></i></a>
                    <a href="#"><i class="icon ion-social-twitter fs-4"></i></a>
                    <a href="#"><i class="icon ion-social-instagram fs-4"></i></a>
                    <!-- Copyright notice -->
                    <p class="copyright mt-2">Rolsa Technologies © 2025</p>
                </div>

            </div>
        </div>
    </footer>



        <script>
            document.addEventListener("DOMContentLoaded", function () {
            const body = document.body;

            // Dark Mode
            function toggleDarkMode() {
                // toggles dark mode on body
                body.classList.toggle("dark-mode");
                localStorage.setItem("theme", body.classList.contains("dark-mode") ? "dark" : "light"); // saves the dark mode in local storage so that even if the page reloads, it stays in dark mode
            }
            if (localStorage.getItem("theme") === "dark") {
                body.classList.add("dark-mode");
            }
            window.toggleDarkMode = toggleDarkMode;

            // High Contrast
            function toggleHighContrast() {
                // toggles high contrast mode on body
                body.classList.toggle("high-contrast");
                localStorage.setItem("contrastMode", body.classList.contains("high-contrast") ? "high-contrast" : "normal"); // saves the high contrast mode in local storage so that even if the page reloads, it stays in high contrast mode
            }
            if (localStorage.getItem("contrastMode") === "high-contrast") {
                body.classList.add("high-contrast");
            }
            window.toggleHighContrast = toggleHighContrast;

            // Font Size
            function changeFontSize(action) {
                let currentSize = parseInt(window.getComputedStyle(body).fontSize);
                if (action === 'increase') { // increase font size
                    currentSize += 2;
                } else if (action === 'decrease') { // decrease font size
                    currentSize -= 2;
                }
                body.style.fontSize = currentSize + "px"; // applies the new font size
                localStorage.setItem("fontSize", currentSize + "px"); // saves the font size in local storage so that even if the page reloads, the font size stays the same
            }
            window.changeFontSize = changeFontSize; // make changeFontSize globally accessible
        });

        </script>
    <style>

        body {
            background-color: white;
            color: black;
            transition: background-color 0.3s, color 0.3s;
        }

            /* Dark mode */
            body.dark-mode {
                background-color: #121212; /* Dark background colour */
                color: white; /* White text colour */
            }

                body.dark-mode h1, body.dark-mode h2, body.dark-mode h3, body.dark-mode h4, body.dark-mode h5, body.dark-mode h6,
                body.dark-mode p, body.dark-mode li, body.dark-mode a {
                    color: white; /* Ensures text and links are white in dark mode */
                }

                body.dark-mode a {
                    text-decoration: none;
                }

                    body.dark-mode a:hover {
                        color: #FFD700; /* Highlight colour on hover */
                    }

            /* High contrast mode */
            body.high-contrast {
                background-color: black !important; /* Black background */
                color: yellow !important; /* Yellow text colour */
            }

                /* High contrast mode for text and links */
                body.high-contrast h1, body.high-contrast h2, body.high-contrast h3, body.high-contrast h4, body.high-contrast h5, body.high-contrast h6,
                body.high-contrast p, body.high-contrast li, body.high-contrast a {
                    color: yellow !important; /* Override text colour to yellow */
                }


        /* Adjust font size */
        body {
            transition: font-size 0.3s;
        }

        /* Footer  */
        footer {
            background-color: var(--background-color);
            color: var(--text-color);
            padding: 20px;
            text-align: center;
            transition: background-color 0.3s, color 0.3s;
        }

            footer a {
                color: var(--text-color);
                text-decoration: none;
            }

                footer a:hover {
                    color: #FFD700; /* gold color for links on hover */
                }

        /* Social media icons */
        .social-icons a {
            margin: 0 10px;
            color: var(--text-color);
            font-size: 20px;
            text-decoration: none;
        }

            .social-icons a:hover {
                color: #FFD700;
            }

        /* navigation styles */
        .navbar {
            background-color: #ffffff; /* White background */
            transition: background-color 0.3s;
        }

            .navbar .nav-link {
                color: #000000; /*  text colour for links */
                transition: color 0.3s;
            }

                .navbar .nav-link:hover {
                    color: #007bff; /* Highlight colour on hover */
                }



        .navbar-nav .nav-item {
            margin-right: 15px; /* Space between nav items */
        }

        /* Dark mode navigation */
        body.dark-mode .navbar {
            background-color: #121212 !important; /* Dark background */
        }

            body.dark-mode .navbar .nav-link {
                color: #ffffff !important; /* White text */
            }

                body.dark-mode .navbar .nav-link:hover {
                    color: #FFD700; /* Highlight colour in dark mode */
                }

        /* Dark mode adjustments */
        body.dark-mode .navbar-toggler-icon {
            background-image: url("data:image/svg+xml;charset=utf8,%3Csvg viewBox='0 0 30 30' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath stroke='%23ffffff' stroke-width='2' stroke-linecap='round' stroke-miterlimit='10' d='M4 7h22M4 15h22M4 23h22'/%3E%3C/svg%3E");
        }

        /* High contrast mode navigation */
        body.high-contrast .navbar {
            background-color: black !important; /* Black background in high contrast */
        }

            body.high-contrast .navbar .nav-link {
                color: yellow !important; /* Yellow text colour */
            }

                body.high-contrast .navbar .nav-link:hover {
                    color: yellow !important; /* Maintain yellow colour on hover */
                }

            body.high-contrast .navbar .navbar-toggler-icon {
                background-color: yellow !important; /* Yellow toggler icon */
            }

        /* Dropdown menu styles */
        .navbar .dropdown-menu {
            background-color: #ffffff; /* White background for dropdown */
            border: none; /* Remove border */
        }

        body.dark-mode .navbar .dropdown-menu {
            background-color: #2c2c2c; /* Dark background for dropdown in dark mode */
        }

        body.high-contrast .navbar .dropdown-menu {
            background-color: black !important; /* Black dropdown in high contrast mode */
        }

        .navbar .dropdown-item {
            transition: color 0.3s;
        }

        body.dark-mode .navbar .dropdown-item {
            color: #ffffff; /* White text for dropdown in dark mode */
        }

        body.high-contrast .navbar .dropdown-item {
            color: yellow !important; /* Yellow text in high contrast mode */
        }

        .navbar .dropdown-item:hover {
            background-color: #007bff; /* Hover background colour (dropdown items) */
            color: #ffffff; /* White text colour on hover */
        }

        body.dark-mode .navbar .dropdown-item:hover {
            background-color: #FFD700; /* Highlight with gold in dark mode */
        }

        /* navbar collapses correctly on small screens */
        @@media (max-width: 767px) {
            .navbar-nav {
                text-align: center;
            }

                .navbar-nav .nav-item {
                    margin-right: 0;
                    margin-bottom: 10px; /* Space between nav items for small screens */
                }
        }

        body.dark-mode .privacy-container {
            background-color: black;
        }

        body.high-contrast .privacy-container {
            background-color: black;
        }


        body.dark-mode .privacy-title {
            color: #fff;
        }

        body.high-contrast .privacy-title {
            color: yellow !important;
        }

        /*Admin Page*/

        /* Table Styling for Accessibility */
        .accessible-table {
            border-collapse: collapse;
            width: 100%;
        }

            .accessible-table th, .accessible-table td {
                padding: 12px;
                border: 1px solid #dee2e6;
            }

            /* Improved Focus Visibility */
            .accessible-table tr:focus {
                outline: 2px solid #007bff;
                background-color: rgba(0, 123, 255, 0.1);
            }

        /* Light Mode Default Styling */
        .dark-mode .accessible-btn {
            color: #007bff; /* Default blue */
        }

        /* Dark Mode Adjustments */
        @@media (prefers-color-scheme: dark) {
            .accessible-table {
                background-color: #1e1e1e;
                color: white;
            }

                .accessible-table th, .accessible-table td {
                    border-color: #444;
                }

                .accessible-table tr:nth-child(even) {
                    background-color: #2a2a2a;
                }

                .accessible-table tr:focus {
                    background-color: rgba(255, 255, 255, 0.2);
                    outline: 2px solid white;
                }
        }


        .dark-mode .btn-outline-secondary {
            color: black !important;
            border-color: black !important;
        }

        /*Green Energy Products Page*/

        /* Dark Mode */
        body.dark-mode {
            background-color: #121212;
            color: white;
        }

            body.dark-mode .card {
                background-color: #1e1e1e;
                color: white;
                border: 1px solid #333;
            }

            body.dark-mode .btn-primary {
                background-color: #FFD700;
                border-color: #FFD700;
                color: black;
            }

                body.dark-mode .btn-primary:hover {
                    background-color: #e6c200;
                    border-color: #e6c200;
                }

        /* High Contrast Mode */
        body.high-contrast {
            background-color: black !important;
            color: yellow !important;
        }

            body.high-contrast .card {
                background-color: black !important;
                color: yellow !important;
                border: 2px solid yellow !important;
            }

            body.high-contrast .btn-primary {
                background-color: yellow !important;
                color: black !important;
                border: 2px solid yellow !important;
            }

        /*Solar Panel Information Page*/



        body.dark-mode {
            background-color: #121212;
            color: white;
        }

            body.dark-mode .card, body.dark-mode .carousel-item {
                background-color: #1e1e1e;
                color: white;
            }

        .carousel img {
            height: 400px;
            object-fit: cover;
        }

        /* EvCharger Information Page */

        body.dark-mode {
            background-color: #121212; /* Dark background colour */
            color: white; /* White text colour */
        }

        .card {
            background-color: #f8f9fa;
            transition: background-color 0.3s, color 0.3s;
        }

            .card h2 {
                font-size: 1.5rem;
            }

        /* Calculator */

        /* CSS to change the text color to white in dark mode */
        body.dark-mode .dark-mode-btn {
            color: white !important;
            border-color: white !important;
        }

            body.dark-mode .dark-mode-btn i {
                color: white !important;
            }

        /* Optional: Set background color for dark mode */
        body.dark-mode .dark-mode-btn {
            background-color: transparent !important; /* or adjust the background color to fit dark mode */
        }
    </style>
     

    <!-- Cookie and Terms Popup -->
    <div id="popup" class="popup">
        <div class="popup-content">
            <h2>Cookies & Terms and Conditions</h2>
            <p>We use cookies to ensure you get the best experience on our website. By continuing, you agree to our <a asp-controller="Home" asp-action="TermsConditions">Terms and Conditions</a> and <a asp-controller="Home" asp-action="Privacy">Privacy Policy</a>.</p>
            <button id="accept-btn">Accept</button>
        </div>
    </div>

    <script>
        // Wait for the page to load
        window.onload = function() {
            // Check if the user has already accepted the terms
            if (!sessionStorage.getItem('termsAccepted')) {
                // Set a timeout to show the popup after 3 seconds
                setTimeout(function() {
                    let popup = document.getElementById('popup');
                    popup.style.display = 'block'; // Show the popup
                }, 3000); // 3000 milliseconds = 3 seconds
            }

            // Get the accept button
            let acceptBtn = document.getElementById('accept-btn');

            // Event listener for the accept button
            acceptBtn.addEventListener('click', function() {
                let popup = document.getElementById('popup');
                popup.style.display = 'none'; // Hide the popup when accepted
                // Set a session storage item to remember the user's acceptance for the session
                sessionStorage.setItem('termsAccepted', 'true');
            });
        };
    </script>

    <style>
        body {
            background-color: white;
            color: black;
            transition: background-color 0.3s, color 0.3s;
        }

            /* Dark mode */
            body.dark-mode {
                background-color: #121212; /* Dark background colour */
                color: white; /* White text colour */
            }

                body.dark-mode h1, body.dark-mode h2, body.dark-mode h3, body.dark-mode h4, body.dark-mode h5, body.dark-mode h6,
                body.dark-mode p, body.dark-mode li, body.dark-mode a {
                    color: white; /* Ensures text and links are white in dark mode */
                }

                body.dark-mode a {
                    text-decoration: none;
                }

                    body.dark-mode a:hover {
                        color: #FFD700; /* Highlight colour on hover */
                    }

            /* High contrast mode */
            body.high-contrast {
                background-color: black !important; /* Black background */
                color: yellow !important; /* Yellow text colour */
            }

                /* High contrast mode for text and links */
                body.high-contrast h1, body.high-contrast h2, body.high-contrast h3, body.high-contrast h4, body.high-contrast h5, body.high-contrast h6,
                body.high-contrast p, body.high-contrast li, body.high-contrast a {
                    color: yellow !important; /* Override text colour to yellow */
                }


        /* Adjust font size */
        .body {
            transition: font-size 0.3s;
        }

        /* Footer  */
        .footer {
            background-color: var(--background-color);
            color: var(--text-color);
            padding: 20px;
            text-align: center;
            transition: background-color 0.3s, color 0.3s;
        }

            footer a {
                color: var(--text-color);
                text-decoration: none;
            }

                footer a:hover {
                    color: #FFD700; /* gold color for links on hover */
                }

        /* Social media icons */
        .social-icons a {
            margin: 0 10px;
            color: var(--text-color);
            font-size: 20px;
            text-decoration: none;
        }

            .social-icons a:hover {
                color: #FFD700;
            }

        /* navigation styles */
        .navbar {
            background-color: #ffffff; /* White background */
            transition: background-color 0.3s;
        }

            .navbar .nav-link {
                color: #000000; /*  text colour for links */
                transition: color 0.3s;
            }

                .navbar .nav-link:hover {
                    color: #007bff; /* Highlight colour on hover */
                }



        .navbar-nav .nav-item {
            margin-right: 15px; /* Space between nav items */
        }

        /* Dark mode navigation */
        body.dark-mode .navbar {
            background-color: #121212 !important; /* Dark background */
        }

            body.dark-mode .navbar .nav-link {
                color: #ffffff !important; /* White text */
            }

                body.dark-mode .navbar .nav-link:hover {
                    color: #FFD700; /* Highlight colour in dark mode */
                }

        /* Dark mode adjustments */
        body.dark-mode .navbar-toggler-icon {
            background-image: url("data:image/svg+xml;charset=utf8,%3Csvg viewBox='0 0 30 30' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath stroke='%23ffffff' stroke-width='2' stroke-linecap='round' stroke-miterlimit='10' d='M4 7h22M4 15h22M4 23h22'/%3E%3C/svg%3E");
        }

        /* High contrast mode navigation */
        body.high-contrast .navbar {
            background-color: black !important; /* Black background in high contrast */
        }

            body.high-contrast .navbar .nav-link {
                color: yellow !important; /* Yellow text colour */
            }

                body.high-contrast .navbar .nav-link:hover {
                    color: yellow !important; /* Maintain yellow colour on hover */
                }

            body.high-contrast .navbar .navbar-toggler-icon {
                background-color: yellow !important; /* Yellow toggler icon */
            }

        /* Dropdown menu styles */
        .navbar .dropdown-menu {
            background-color: #ffffff; /* White background for dropdown */
            border: none; /* Remove border */
        }

        body.dark-mode .navbar .dropdown-menu {
            background-color: #2c2c2c; /* Dark background for dropdown in dark mode */
        }

        body.high-contrast .navbar .dropdown-menu {
            background-color: black !important; /* Black dropdown in high contrast mode */
        }

        .navbar .dropdown-item {
            transition: color 0.3s;
        }

        body.dark-mode .navbar .dropdown-item {
            color: #ffffff; /* White text for dropdown in dark mode */
        }

        body.high-contrast .navbar .dropdown-item {
            color: yellow !important; /* Yellow text in high contrast mode */
        }

        .navbar .dropdown-item:hover {
            background-color: #007bff; /* Hover background colour (dropdown items) */
            color: #ffffff; /* White text colour on hover */
        }

        body.dark-mode .navbar .dropdown-item:hover {
            background-color: #FFD700; /* Highlight with gold in dark mode */
        }

        /* navbar collapses correctly on small screens */
        @@media (max-width: 767px) {
            .navbar-nav {
                text-align: center;
            }

                .navbar-nav .nav-item {
                    margin-right: 0;
                    margin-bottom: 10px; /* Space between nav items for small screens */
                }
        }

        body.dark-mode .privacy-container {
            background-color: black;
        }

        body.high-contrast .privacy-container {
            background-color: black;
        }


        body.dark-mode .privacy-title {
            color: #fff;
        }

        body.high-contrast .privacy-title {
            color: yellow !important;
        }

        /*Admin Page*/

        /* Table Styling for Accessibility */
        .accessible-table {
            border-collapse: collapse;
            width: 100%;
        }

            .accessible-table th, .accessible-table td {
                padding: 12px;
                border: 1px solid #dee2e6;
            }

            /* Improved Focus Visibility */
            .accessible-table tr:focus {
                outline: 2px solid #007bff;
                background-color: rgba(0, 123, 255, 0.1);
            }

        /* Light Mode Default Styling */
        .dark-mode .accessible-btn {
            color: #007bff; /* Default blue */
        }

        /* Dark Mode Adjustments */
        @@media (prefers-color-scheme: dark) {
            .accessible-table {
                background-color: #1e1e1e;
                color: white;
            }

                .accessible-table th, .accessible-table td {
                    border-color: #444;
                }

                .accessible-table tr:nth-child(even) {
                    background-color: #2a2a2a;
                }

                .accessible-table tr:focus {
                    background-color: rgba(255, 255, 255, 0.2);
                    outline: 2px solid white;
                }
        }


        .dark-mode .btn-outline-secondary {
            color: black !important;
            border-color: black !important;
        }

        /*Green Energy Products Page*/

        /* Dark Mode */
        body.dark-mode {
            background-color: #121212;
            color: white;
        }

            body.dark-mode .card {
                background-color: #1e1e1e;
                color: white;
                border: 1px solid #333;
            }

            body.dark-mode .btn-primary {
                background-color: #FFD700;
                border-color: #FFD700;
                color: black;
            }

                body.dark-mode .btn-primary:hover {
                    background-color: #e6c200;
                    border-color: #e6c200;
                }

        /* High Contrast Mode */
        body.high-contrast {
            background-color: black !important;
            color: yellow !important;
        }

            body.high-contrast .card {
                background-color: black !important;
                color: yellow !important;
                border: 2px solid yellow !important;
            }

            body.high-contrast .btn-primary {
                background-color: yellow !important;
                color: black !important;
                border: 2px solid yellow !important;
            }

        /*Solar Panel Information Page*/



        body.dark-mode {
            background-color: #121212;
            color: white;
        }

            body.dark-mode .card, body.dark-mode .carousel-item {
                background-color: #1e1e1e;
                color: white;
            }

        .carousel img {
            height: 400px;
            object-fit: cover;
        }

        /* EvCharger Information Page */

        body.dark-mode {
            background-color: #121212; /* Dark background colour */
            color: white; /* White text colour */
        }

        .card {
            background-color: #f8f9fa;
            transition: background-color 0.3s, color 0.3s;
        }

            .card h2 {
                font-size: 1.5rem;
            }

        /* Calculator */

        /* CSS to change the text color to white in dark mode */
        body.dark-mode .dark-mode-btn {
            color: white !important;
            border-color: white !important;
        }

            body.dark-mode .dark-mode-btn i {
                color: white !important;
            }

        /* Optional: Set background color for dark mode */
        body.dark-mode .dark-mode-btn {
            background-color: transparent !important; /* or adjust the background color to fit dark mode */
        }


        /* Basic styling for the popup */
        .popup {
            position: fixed;
            bottom: 20px;
            left: 50%;
            transform: translateX(-50%);
            background-color: rgba(0, 0, 0, 0.8);
            color: white;
            padding: 20px;
            border-radius: 10px;
            display: none; /* Hidden by default */
            z-index: 9999;
            width: 80%;
            max-width: 400px;
            text-align: center;
        }

        .popup-content {
            padding: 10px;
        }

        button {
            background-color: #4CAF50;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

            button:hover {
                background-color: #45a049;
            }

        a {
            color: #4CAF50;
        }

    </style>
    
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
